name: Publish Project Production In AWS

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        env:
            PRIVATE_KEY: ${{ secrets.AWS_SSH_KEY }}
            HOSTNAME: ${{ secrets.EC2_HOST }}
            USER_NAME: ec2-user

      - name: Debug Information
        run: |
           echo "Current Directory: $(pwd)"
           echo "Contents of Directory: $(ls)"
           echo "Value of Variable XYZ: $XYZ"      

      - name: Verify Secret Value
        run: | 
            echo "$PRIVATE_KEY"  # This will print the private key, useful for debugging

      - name: Configure SSH and Add Private Key to SSH Agent
        run: |
          mkdir -p ~/.ssh
          echo "$PRIVATE_KEY" > ~/.ssh/id_rsa
          chmod 400 ~/.ssh/id_rsa 
          ssh-keyscan $HOSTNAME >> ~/.ssh/known_hosts
          echo "StrictHostKeyChecking no" >> ~/.ssh/config

      - name: Start SSH Agent and Add Private Key
        run: |
            eval $(ssh-agent -s)
            ssh-add ~/.ssh/id_rsa

      - name: SSH into EC2 and Execute Commands
        run: |
          ssh -o StrictHostKeyChecking=no ${USER_NAME}@${HOSTNAME} '
          echo "Hello Git Actions"'
